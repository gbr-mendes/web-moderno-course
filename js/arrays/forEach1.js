const aprovados = ['Agatha', 'Aldo', 'Daniel', 'Raquel']

aprovados.forEach((nome, indice)=>console.log(`${indice + 1}) ${nome}`))
// forEach pode receber 3 parametros valor do item do array, index desse item e o prorio array